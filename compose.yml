name: image_view
services:
  image_view_website:
    container_name: image_view
    ports:
      - 5000:5000
    volumes:
      - pictures:/uploads
    environment:
      FLASK_SECRET_KEY: lsdkjffodsfj0wes
    image: image_view

volumes:
  pictures: